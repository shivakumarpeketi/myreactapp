import React from 'react'

function FunctionalComponentF() {
    return (
        <div>
            <button onClick={()=> 0}>Increase</button>
            <button onClick={()=> 0}>Decrease</button>
            <button onClick={()=> 0}>Reset</button>
        </div>
    )
}

export default FunctionalComponentF
